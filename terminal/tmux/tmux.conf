# Enable True color support
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",tmux-256color:RGB,xterm-256color:RGB"

set -g history-limit 100000
set -g escape-time 0
set -g focus-events on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Windows are named after whatever is running in them (see
# automatic-rename-format below), which is what the icon map keys off.
set -g automatic-rename on
set -g allow-rename off

# Publish the session name as the terminal title, so the kitty tab holding this
# session is labelled with it (kitty renders it through tab_title_template, as
# "1: dotfiles"). It tracks renames and session switches, not just the launch.
set -g set-titles on
set -g set-titles-string "#S"


##### Prefix / key bindings #####
# Change prefix from Ctrl-b to Ctrl-Space
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Reload config quickly: Prefix + r
# Via reload.sh rather than source-file directly, so that a flavor change is
# actually picked up on a running server. See the script.
bind r run-shell '~/.tmux/scripts/reload.sh' \; display-message "tmux.conf reloaded"

# Kill pane without confirmation: Prefix + x
bind x kill-pane

# New named session without leaving the current one: Prefix + N prompts for a
# name in a centered popup, then spawns it and switches to it. A popup rather
# than command-prompt because status-justify 'centre' splits the built-in prompt
# across the bar (label in the middle, input over the session pill). Capital N so
# it doesn't clash with next-window navigation.
bind N display-popup -w 30% -h 3 -x C -y C -E \
	'printf "New session name: "; read name; [ -n "$name" ] && tmux new-session -d -s "$name" && tmux switch-client -t "$name"'

##### Splits #####
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# Vim-style pane movement (Alt + hjkl)
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# Resize panes with Shift + hjkl
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

##### Popups #####
# Prefix + t → terminal, Prefix + g → git. Both centered at 90%, opened in the
# directory of the pane you pressed them from.
unbind t
bind-key t display-popup -w 90% -h 90% -x C -y C -d "#{pane_current_path}" "$SHELL -i"

unbind g
bind-key g display-popup -w 90% -h 90% -x C -y C -d "#{pane_current_path}" -E "lazygit"

# Via launch.sh, not btop directly: the popup runs through sh, so the btop shell
# function that picks the light/dark theme does not exist here. See the script.
unbind b
bind-key b display-popup -w 90% -h 90% -x C -y C -E "~/.config/btop/launch.sh"

##### Mouse #####
set -g mouse on

##### Copy mode (vi-style) #####
setw -g mode-keys vi
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi y send -X copy-selection-and-cancel
bind -T copy-mode-vi C-d send -X halfpage-down
bind -T copy-mode-vi C-u send -X halfpage-up

# tmux plugin manager
set-environment -g TMUX_PLUGIN_MANAGER_PATH ~/.tmux/plugins
set -g @plugin 'tmux-plugins/tpm' # must be the first plugin

set -g @plugin 'omerxx/tmux-sessionx' # default keybind "<prefix> O" to activate

set -g @plugin 'catppuccin/tmux' # palette source for the @thm_* colors below

set -g @sessionx-bind 'o'

##### Theme #####
# Follow the OS appearance, exactly like kitty already does via
# light-theme.auto.conf / dark-theme.auto.conf. This has to run before the
# catppuccin plugin loads, which is why the tpm `run` line stays at the very
# bottom of this file.
if-shell '[ "$(~/.tmux/scripts/flavor.sh)" = latte ]' \
	'set -g @catppuccin_flavor latte' \
	'set -g @catppuccin_flavor macchiato'

# Publishes the pill caps and Nerd Font icons as @cap_* / @ico_* options. tmux
# config has no \u escape, so this keeps private-use codepoints out of this file
# where they would be invisible and easy to lose. See scripts/icons.sh.
run-shell '~/.tmux/scripts/icons.sh'

set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"
# Border colors must go through the plugin's own options, not pane-border-style
# directly: tpm loads catppuccin at the bottom of this file and overwrites the
# tmux options, so anything set here by hand is discarded. @thm_fg is the
# palette's foreground -- near-white in Macchiato, near-black in Latte -- so the
# focused pane stays the high-contrast one in both flavors.
set -g @catppuccin_pane_border_style "fg=#{@thm_overlay_0}"
set -g @catppuccin_pane_active_border_style "fg=#{@thm_fg}"

##### Panes #####
# Which pane has focus, at a glance. tmux borders are always one cell wide --
# `heavy` picks the thicker box-drawing glyphs, which is as bold as it gets.
# (Colors are set above, via the @catppuccin_* options.)
# With exactly two panes tmux colours only *half* the divider as its focus hint
# (pane-border-indicators, default `colour`). Turn that off so the whole border
# takes the active style -- the half-lit line reads as a rendering glitch.
set -g pane-border-indicators off
set -g pane-border-lines heavy

##### Status bar #####
# The bar is transparent and every module is a rounded pill floating on the
# terminal background. Modules live in ~/.tmux/scripts and print their own
# styling; see scripts/lib.sh for why the colors are passed as arguments.
set -g status-position top
set -g status-style "bg=default"
# 'centre' rather than 'absolute-centre': absolute-centre pins the window list
# to the exact middle of the terminal regardless of what is beside it, so on a
# narrow client it gets drawn straight over the right-hand modules.
set -g status-justify "centre"
set -g status-interval 5

# Nerd Font icon for whatever the window is running. Kept in an option so the
# status formats below stay readable; #{E:@win_icon} expands it per window.
set -g @win_icon "#{?#{m:[0-9]*.[0-9]*.[0-9]*,#{pane_current_command}},#{@ico_claude},\
#{?#{m:*nvim*,#{pane_current_command}},#{@ico_nvim},\
#{?#{m:*vim*,#{pane_current_command}},#{@ico_vim},\
#{?#{==:#{pane_current_command},lazygit},#{@ico_git},\
#{?#{==:#{pane_current_command},git},#{@ico_git},\
#{?#{==:#{pane_current_command},go},#{@ico_go},\
#{?#{m:node*,#{pane_current_command}},#{@ico_node},\
#{?#{m:*npm*,#{pane_current_command}},#{@ico_node},\
#{?#{m:python*,#{pane_current_command}},#{@ico_python},\
#{?#{==:#{pane_current_command},docker},#{@ico_docker},\
#{?#{==:#{pane_current_command},ssh},#{@ico_ssh},\
#{?#{==:#{pane_current_command},yazi},#{@ico_folder},\
#{?#{m:java*,#{pane_current_command}},#{@ico_java},\
#{?#{m:*sh,#{pane_current_command}},#{@ico_shell},\
#{@ico_default}}}}}}}}}}}}}}}"

# Claude Code state, published per pane by scripts/claude-state.sh from Claude's
# own hooks (see the script). Rendered twice: as a dot on the window chip, and
# as the agents pill in status-left that covers the sessions you cannot see.
#
# @claude_live: does this pane hold a Claude that has reported a state? The
# command check is the crash guard -- a state left behind when SessionEnd never
# ran disappears as soon as the pane runs anything else. Same semver-argv0 match
# the @win_icon map keys off.
set -g @claude_live "#{&&:#{m:[0-9]*.[0-9]*.[0-9]*,#{pane_current_command}},\
#{!=:#{@claude_state},}}"

# Glyph and colour are chosen separately, because the same state is drawn on two
# different backgrounds: the terminal background / pill surface, where the raw
# accent is what the whole bar uses, and the active window chip, which is a
# filled mauve badge. On Macchiato that badge and the accents are both light
# pastels and the accent lands near 1.5:1 on it -- the dot is there and you
# cannot see it. @badge_icon_* is the same accent re-derived to 3:1 against that
# fill; see palette.sh.
set -g @claude_ico "#{?#{==:#{@claude_state},busy},#{@ico_claude_busy},\
#{?#{==:#{@claude_state},wait},#{@ico_claude_wait},\
#{@ico_claude_idle}}}"

set -g @claude_col "#{?#{==:#{@claude_state},busy},#{@icon_peach},\
#{?#{==:#{@claude_state},wait},#{@icon_red},\
#{@icon_green}}}"

set -g @claude_col_badge "#{?#{==:#{@claude_state},busy},#{@badge_icon_peach},\
#{?#{==:#{@claude_state},wait},#{@badge_icon_red},\
#{@badge_icon_green}}}"

# Styles stay single-attribute (#[fg=...] only): a comma inside #[...] would be
# read as a conditional separator by the parser.
set -g @claude_glyph "#[fg=#{E:@claude_col}]#{E:@claude_ico}"

# Appended to the window chip: one for the quiet chips, one for the active
# badge. The glyph is the same, only the colour is re-derived.
#
# Per pane, not per window. @claude_state is a *pane* option, and a window
# format resolves pane options against the window's active pane -- so a Claude
# in a background split simply stopped being reported the moment you focused
# the shell beside it. #{P:...} walks the window's panes instead, which also
# means a window running two agents gets two dots. Each glyph brings its own
# leading space, so a window with no agent adds nothing at all.
set -g @claude_dot "#{P:#{?#{E:@claude_live}, #{E:@claude_glyph},}}"
set -g @claude_dot_badge "#{P:#{?#{E:@claude_live}, #[fg=#{E:@claude_col_badge}]#{E:@claude_ico},}}"

# Agents pill: every Claude in every *other* session, as a dot and a name.
#
# The whole point is the tabs you cannot see. Each kitty tab is its own client
# attached to its own session, but they all share one tmux server -- so a status
# line can walk the entire server and report on sessions that are nowhere on
# screen. #{S:...} loops over sessions and #{W:...} over their windows, which is
# what makes this cost nothing: it is a plain format, expanded on redraw, with
# no #() job, no polling and no state of its own.
#
# #{client_session} survives inside the loop and names the session this client
# is showing, so each tab drops itself from the list -- its own agent is already
# on the window chip a few columns to the right.
#
# A session is named once and carries one glyph per Claude in it, so two agents
# in one project read as 'wms x2' rather than repeating the name. The glyph run
# is built first, in @claude_session_dots, because the name may only be printed
# when that run is non-empty -- expanding it with #{E:} inside the session loop
# is what makes 'does this session have any agents at all' answerable.
#
# Three loops deep: sessions, their windows, and the windows' panes. The last
# one is not pedantry -- a window reports its active pane, so without #{P:} an
# agent sharing a window with a shell disappears from the bar as soon as the
# shell has focus.
#
# Group width is the name plus one glyph per agent, whatever the states are, so
# a busy -> wait flip repaints in place. The pill only changes width when an
# agent appears or disappears, which is the one time the centred window list may
# shift a column.
set -g @claude_session_dots "#{W:#{P:#{?#{E:@claude_live},#{E:@claude_glyph} ,}}}"

# Spacing carries the grouping, since there is nothing else to hold it: one
# space after each dot, one before each group, so agents within a session sit a
# column apart and two sessions sit two columns apart. It also pads the pill
# evenly against its caps.
#
# A drawn separator would have to be stripped off the last group -- a format
# loop can only append -- and the #{s/...$//:} that would do it has to match the
# style tag in front of the glyph, which means hardcoding one flavor's hex. Two
# spaces cost nothing and survive a palette change.
set -g @claude_agents "#{S:#{?#{==:#{session_name},#{client_session}},,\
#{?#{==:#{E:@claude_session_dots},},,\
 #[fg=#{@text_mauve}]#{=10:#{session_name}} #{E:@claude_session_dots}}}}"

# Wrapped in a pill only when it has something to say, like the module scripts:
# #{E:...} expands the list so the emptiness test sees the rendered text.
#
# One attribute per #[...] here, where every other pill in this file writes
# #[fg=x,bg=y]: this whole thing sits inside a conditional, and the parser
# splits branches on the first top-level comma it sees -- including the one in
# the middle of a style. It renders as half a pill and stray 'bg=default]' text.
set -g @claude_agents_pill "#{?#{==:#{E:@claude_agents},},,\
#[fg=#{@fill_mauve}]#[bg=default]#{@cap_l}\
#[bg=#{@fill_mauve}]#[bold]#[fg=#{@icon_mauve}]#{@ico_claude}\
#{E:@claude_agents}\
#[fg=#{@fill_mauve}]#[bg=default]#[nobold]#{@cap_r} }"

# status left: session pill (red while the prefix is held) + zoom pill + agents
# 150, not the old 100: the agents pill grows with the number of Claudes on the
# server, and tmux truncates status-left silently rather than wrapping it.
set -g status-left-length 150
set -g status-left ""
# Zero-width: reloads the config when the OS appearance flips. See the script.
set -ga status-left "#(~/.tmux/scripts/theme-watch.sh)"
# An emphasis pill, so it takes @badge_* rather than the grey @fill_* the info
# pills use: a filled accent on dark, a pale wash of it on light. Both carry
# @badge_fg_*, so this needs no branching. See scripts/palette.sh.
set -ga status-left "#[fg=#{?client_prefix,#{@badge_red},#{@badge_green}},bg=default]#{@cap_l}\
#[fg=#{?client_prefix,#{@badge_fg_red},#{@badge_fg_green}},bg=#{?client_prefix,#{@badge_red},#{@badge_green}},bold]\
#{?client_prefix,#{@ico_prefix},#{@ico_session}} #S\
#[fg=#{?client_prefix,#{@badge_red},#{@badge_green}},bg=default,nobold]#{@cap_r} "
set -ga status-left "#{?window_zoomed_flag,\
#[fg=#{@fill_yellow}#,bg=default]#{@cap_l}\
#[bg=#{@fill_yellow}#,bold]#[fg=#{@icon_yellow}]#{@ico_zoom}\
#[fg=#{@text_yellow}] zoom\
#[fg=#{@fill_yellow}#,bg=default#,nobold]#{@cap_r} ,}"
set -ga status-left "#{E:@claude_agents_pill}"

# status right: git · docker · cpu · mem
#
# One job, not one per module. tmux redraws the status line every time a #()
# finishes, so a job apiece meant a repaint per module per interval, each a
# different width — and with status-justify 'centre' that drags the window list
# sideways on every one of them. status.sh runs the modules and emits the whole
# right side in a single write. See the script.
set -g status-right-length 250
set -g status-right ""
set -ga status-right "#(~/.tmux/scripts/status.sh '#{pane_current_path}' \
'#{@thm_surface_0}' '#{@icon_green}' '#{@icon_peach}' '#{@icon_sapphire}' \
'#{@icon_mauve}' '#{@icon_blue}' '#{@pill_text}')"
# No date, clock, battery or network pill: macOS already draws all four in its
# own menu bar, a few pixels above this one, and a status bar that repeats the
# system one is just noise. The modules and the @ico_date / @ico_clock glyphs
# are still here; see the README for putting any of them back.

# window look and feel: inactive windows are quiet text, the active one is a
# filled pill, so focus is obvious at a glance.
# Claude Code overwrites its own argv[0] with a bare version string ("2.1.212"),
# which tmux reads as the command name and would otherwise show as the window
# name. Relabel that semver shape as 'claude'; every other command keeps its own
# name. Same pattern the @win_icon map keys off to give it the robot glyph.
set -g automatic-rename-format "#{?#{m:[0-9]*.[0-9]*.[0-9]*,#{pane_current_command}},claude,#{pane_current_command}}"

set -g window-status-format "#[fg=#{@thm_overlay_1},bg=default] #I #{E:@win_icon} #W#{E:@claude_dot} "
# The badge pads itself on both sides, where it used to sit flush against its
# caps. An MDI glyph draws wider than the cell tmux measures it as, so the round
# right cap clipped the dot; and padding only that side left the chip visibly
# lopsided on windows that had one. A space at each end is symmetric whether or
# not the window holds an agent.
set -g window-status-current-format "#[fg=#{@badge_mauve},bg=default]#{@cap_l}\
#[fg=#{@badge_fg_mauve},bg=#{@badge_mauve},bold] #I #{E:@win_icon} #W#{E:@claude_dot_badge} \
#[fg=#{@badge_mauve},bg=default,nobold]#{@cap_r}"

set -g window-status-style "bg=default,fg=#{@thm_overlay_0}"
set -g window-status-last-style "bg=default,fg=#{@thm_overlay_1}"
set -g window-status-activity-style "bg=default,fg=#{@thm_red},bold"
set -g window-status-bell-style "bg=default,fg=#{@thm_red},bold"
set -g window-status-separator ""

# pane border look and feel
setw -g pane-border-status top
setw -g pane-border-format "#{?pane_active,#[fg=#{@thm_blue},bg=default]#[fg=#{@thm_bg},bg=#{@thm_blue}] #P #{pane_current_command} #[fg=#{@thm_blue},bg=default],#[fg=#{@thm_overlay_0}] #P #{pane_current_command} }"
setw -g pane-active-border-style "bg=default,fg=#{@thm_overlay_1}"
setw -g pane-border-style "bg=default,fg=#{@thm_overlay_0}"

# Must stay last: tpm loads catppuccin, which reads @catppuccin_flavor set
# above.
run '~/.tmux/plugins/tpm/tpm'

# After tpm, not before: this reads the @thm_* palette catppuccin publishes and
# derives the pill colors from it. The formats above reference the @badge_* /
# @fill_* / @icon_* / @text_* it sets, which tmux expands lazily on each redraw
# — so setting them after the formats are defined is fine.
run-shell '~/.tmux/scripts/palette.sh'

# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 8

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
# Keep monitor-level mouse follow, but disable per-window cursor jumping.
# on-focus-changed = "move-mouse window-lazy-center"

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 2
inner.vertical =   2
outer.left =       0
outer.bottom =     0
outer.top =        0
outer.right =      0

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# You can uncomment this line to open up terminal with alt + enter shortcut
# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# alt-enter = 'exec-and-forget open -n /System/Applications/Utilities/Terminal.app'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
cmd-alt-h = 'focus left'
cmd-alt-j = 'focus down'
cmd-alt-k = 'focus up'
cmd-alt-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
cmd-ctrl-alt-shift-h = 'move left'
cmd-ctrl-alt-shift-j = 'move down'
cmd-ctrl-alt-shift-k = 'move up'
cmd-ctrl-alt-shift-l = 'move right'


# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-a = ['workspace A', 'exec-and-forget /opt/homebrew/bin/aerospace list-windows --all --format "%{window-title}" | grep -q "^herdr$" || open -na kitty --args --title herdr /opt/homebrew/bin/herdr'] # herdr
alt-b = ['workspace B', 'exec-and-forget open -b com.microsoft.edgemac || open -b com.brave.Browser'] # browser: edge (work), else brave
alt-c = ['workspace C', 'exec-and-forget open -b com.google.Chrome'] # chrome
alt-d = ['workspace D', 'exec-and-forget open -b com.jetbrains.datagrip'] # datagrip
alt-e = ['workspace E', 'exec-and-forget open -b com.apple.finder'] # finder
alt-g = ['workspace G', 'exec-and-forget open -b com.openai.chat'] # chatgpt
alt-i = ['workspace I', 'exec-and-forget open -b com.jetbrains.intellij || open -b com.jetbrains.intellij.ce'] # intellij: ultimate, else ce
alt-o = ['workspace O', 'exec-and-forget open -b org.keepassxc.keepassxc'] # keepassxc
alt-p = ['workspace P', 'exec-and-forget open -b com.postmanlabs.mac'] # postman
alt-m = ['workspace M', 'exec-and-forget open -b com.microsoft.Outlook || open -b com.apple.mail'] # mail: outlook (work), else apple mail
alt-n = ['workspace N', 'exec-and-forget open -b md.obsidian'] # obsidian
alt-s = ['workspace S', 'exec-and-forget open -b com.apple.Safari'] # safari
alt-t = ['workspace T', 'exec-and-forget open -b net.kovidgoyal.kitty'] # terminal
alt-u = ['workspace U', "exec-and-forget open -a 'Telegram Desktop'"] # telegram
alt-y = ['workspace Y', 'exec-and-forget open -b com.microsoft.teams2'] # teams

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
cmd-ctrl-alt-shift-1 = 'move-node-to-workspace 1'
cmd-ctrl-alt-shift-2 = 'move-node-to-workspace 2'
cmd-ctrl-alt-shift-3 = 'move-node-to-workspace 3'
cmd-ctrl-alt-shift-a = 'move-node-to-workspace A'
cmd-ctrl-alt-shift-b = 'move-node-to-workspace B'
cmd-ctrl-alt-shift-c = 'move-node-to-workspace C'
cmd-ctrl-alt-shift-d = 'move-node-to-workspace D'
cmd-ctrl-alt-shift-e = 'move-node-to-workspace E'
cmd-ctrl-alt-shift-g = 'move-node-to-workspace G'
cmd-ctrl-alt-shift-i = 'move-node-to-workspace I'
cmd-ctrl-alt-shift-p = 'move-node-to-workspace P'
cmd-ctrl-alt-shift-m = 'move-node-to-workspace M'
cmd-ctrl-alt-shift-n = 'move-node-to-workspace N'
cmd-ctrl-alt-shift-s = 'move-node-to-workspace S'
cmd-ctrl-alt-shift-t = 'move-node-to-workspace T'
cmd-ctrl-alt-shift-u = 'move-node-to-workspace U'
cmd-ctrl-alt-shift-y = 'move-node-to-workspace Y'

cmd-ctrl-alt-shift-f = 'fullscreen'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
cmd-ctrl-alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
cmd-ctrl-alt-shift-semicolon = 'mode service'

cmd-ctrl-alt-shift-r = 'mode resize'

[mode.resize.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'


# See: https://nikitabobko.github.io/AeroSpace/commands#resize
minus = 'resize smart -50'
equal = 'resize smart +50'

enter = 'mode main'
esc = 'mode main'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

h = ['join-with left', 'mode main']
j = ['join-with down', 'mode main']
k = ['join-with up', 'mode main']
l = ['join-with right', 'mode main']

[[on-window-detected]]
if.app-id = 'net.kovidgoyal.kitty'
if.window-title-regex-substring = 'herdr'
run = "move-node-to-workspace A"

[[on-window-detected]]
if.app-id = 'net.kovidgoyal.kitty'
run = "move-node-to-workspace T"

[[on-window-detected]]
if.app-id = 'com.google.Chrome'
run = "move-node-to-workspace C"

[[on-window-detected]]
if.app-id = 'com.jetbrains.datagrip'
run = "move-node-to-workspace D"

[[on-window-detected]]
if.app-id = 'com.apple.Safari'
run = "move-node-to-workspace S"

[[on-window-detected]]
if.app-id = 'com.brave.Browser'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'com.apple.mail'
run = "move-node-to-workspace M"

[[on-window-detected]]
if.app-id = 'md.obsidian'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = "move-node-to-workspace E"

[[on-window-detected]]
if.app-id = 'com.tdesktop.Telegram'
run = "move-node-to-workspace U"

[[on-window-detected]]
if.app-id = 'com.microsoft.teams2'
run = "move-node-to-workspace Y"

[[on-window-detected]]
if.app-id = 'com.openai.chat'
run = "move-node-to-workspace G"

[[on-window-detected]]
if.app-id = 'com.jetbrains.intellij'
run = "move-node-to-workspace I"

[[on-window-detected]]
if.app-id = 'org.keepassxc.keepassxc'
run = "move-node-to-workspace O"

[[on-window-detected]]
if.app-id = 'com.jetbrains.intellij.ce'
run = "move-node-to-workspace I"

[[on-window-detected]]
if.app-id = 'com.microsoft.Outlook'
run = "move-node-to-workspace M"

[[on-window-detected]]
if.app-id = 'com.microsoft.edgemac'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'com.postmanlabs.mac'
run = "move-node-to-workspace P"
